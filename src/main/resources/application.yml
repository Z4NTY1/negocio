spring:
  application:
    name: negocio
  jackson:
    time-zone: America/Bogota
  profiles:
    active: dev   # Cambiar a 'prod' en producción

server:
  port: 8080

cors:
  allowed-origins: http://localhost:3000,http://localhost:4200,http://localhost:5173

# Logs: por defecto se escriben en la carpeta 'logs' (negocio.log y negocio-error.log).
# Para cambiar la ruta: java -DLOG_PATH=C:/ruta/mis-logs -jar negocio.jar
logging:
  level:
    root: INFO
    com.prog.negocio: INFO